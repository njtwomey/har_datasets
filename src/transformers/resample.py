from src import BaseGraph


class Resampler(BaseGraph):
    def __init__(self, name, parent, fs_new):
        raise NotImplementedError


class resample_33(Resampler):
    def __init__(self, name, parent, fs_new):
        raise NotImplementedError
