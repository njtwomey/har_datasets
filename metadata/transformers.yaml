# Filter the static gravity component from the
# dynamic body component
body_grav_filter:
  description:
    -
  resamples: false

# Resample the signals to 33 Hz
resample:
  description:
    -

# Window the data to length of 1.28 s with increments of 1s
window:
  description:
    -
  resamples: true
